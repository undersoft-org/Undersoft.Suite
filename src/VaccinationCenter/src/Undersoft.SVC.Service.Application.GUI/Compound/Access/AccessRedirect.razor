@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"access/sign_in?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
